# Decision Log

Format: date — decision — rationale — status.

| # | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| 1 | 2026-08-04 | Product concept: performance & data-trust cockpit as the spine; ACFR workflow as a supporting drill-down; market context as a separated strip | Works backward from the brief's primary decision question; every widget traces to an observed public-report pattern (see `docs/discovery/concept-and-scope.md`) | Approved (discovery) |
| 2 | 2026-08-04 | Brinson attribution excluded from public v1; weighted contribution with visible reconciliation instead | Synthetic data cannot make allocation/selection/interaction reconcile credibly; contribution can be made airtight and testable | Approved (discovery) |
| 3 | 2026-08-04 | Peer-universe (InvMetrics) content excluded | Licensed data | Approved (discovery) |
| 4 | 2026-08-04 | Synthetic portfolio series are monthly; daily series only in the market-context strip; contribution compounds monthly BOP-weight contributions with a disclosed residual and tolerance | Matches observed reporting cadences; avoids implying daily official performance | Approved (discovery) |
| 5 | 2026-08-04 | No republication of Alpaca-sourced prices or any licensed index series; `reported_public` restricted to individually cited values from LACERA's own public documents | Licensing and provenance discipline | Approved (discovery) |
| 6 | 2026-08-04 | Web stack: Vite + React 18 + TypeScript strict + Zod + Vitest + PapaParse + Recharts; GitHub Pages project site; no backend/secrets/telemetry | See `docs/discovery/architecture-options.md` | Approved (discovery) |
| 7 | 2026-08-04 | Excel bridge is the system of record for demo data; web consumes a normalized export table, never presentation cells | Keeps the analyst-auditable artifact authoritative | Approved (discovery) |
| 8 | 2026-08-04 | Classification model: origin enum (`reported_public`/`synthetic`/`proxy_estimate`/`calculated`) + state overlays (`stale`/`missing`) carried as separate fields | A value can be synthetic *and* stale; conflating origin with state loses information | Approved (discovery) |
| 9 | 2026-08-04 | Starter-workbook defect `Overview!B10` (COUNTA counts table header; completion % caps at 95.8%) fixed only in the derived workbook via structured references | `reference/` is immutable | Confirmed; fix scheduled for Excel stage |
| 10 | 2026-08-04 | Demo dataset: one synthetic entity `DEMOFUND` (+ entity dimension reserved), demo fiscal year Jul 2025–Jun 2026, one daily market-strip month | Smallest history that makes FYTD/QTD/1Y meaningful | Approved (discovery) |
| 11 | 2026-08-04 | Routing: relative asset base (`./`) + HashRouter instead of `basename` path routing | Build becomes path-independent (works on any Pages subpath, no repo-name coupling, no 404 rewrite); `#` URLs acceptable for a demo | Approved (dashboard) — revises decision 6 detail |
| 12 | 2026-08-04 | Fixture consumed via Vite `?raw` import directly from `data/sample/` | Single source of truth; no copy step to drift | Approved (dashboard) |
| 13 | 2026-08-04 | V09 presence requirement scoped to return/contribution record types; balance/market/check records may carry descriptive `period_type` without a span | Matches the contract's conditional-requirement table; the validator originally over-enforced and rejected its own fixture | Approved (dashboard) |
| 14 | 2026-08-04 | `public_reference.entity_id` names the cited public entity, never `DEMOFUND`; V17 exempts citation rows | Quotations must not be attributed to the synthetic fund | Approved (dashboard) — workbook + fixtures regenerated |
| 15 | 2026-08-04 | ACFR workflow board data is app-bundled, outside contract 1.x | Import path deliberately confined to performance records; tracker records are a schema-1.1 candidate | Approved (dashboard) |
| 16 | 2026-08-06 | Policy bands stored as explicit min/target/max, never target ± half-width | Pension IPS Cash is 1% with +2/−1 (0–3%) — symmetric storage produces a wrong upper bound | Approved (revision 2) |
| 17 | 2026-08-06 | Daily EOD story = policy-weighted **proxy read-through** with policy-weight coverage; no simulated NAV-pricing coverage or daily performance estimate on the public site | Coverage in policy-weight terms is derivable and honest; "% of NAV priced" would imply capability the prototype lacks and edge toward an official-performance claim | Approved (revision 2) — reframes the external review's hero mock |
| 18 | 2026-08-06 | Multi-entity import files rejected (V17) until a real fund selector exists; model defensively scopes to one entity | Silently blending funds is worse than refusing the file | Approved (revision 2) |
| 19 | 2026-08-06 | Derived fields (over/under, totals) always recomputed from primitives; partial sleeve totals suppressed with a visible exception | Imported derived values can silently disagree with their inputs | Approved (revision 2) |
| 20 | 2026-08-06 | Source-data inconsistencies are flagged, not corrected (OPEB RAIH ½-step sub-class sum 15.5% vs category 16.5%) | The dashboard's credibility rests on never silently "fixing" a source | Approved (revision 2) |
| 21 | 2026-08-06 | Bloomberg/licensed data workflow is internal-use only: import locally or on an internal host; never committed, never baked into the public site | Terminal data licensing; public site stays synthetic + cited-public | Approved (revision 2) |
| 22 | 2026-08-06 | OPEB demo dataset is generated by the shared Python engine (`tools/make_opeb_fixture.py`), not a second Excel workbook; the workbook remains the auditable Pension demonstration | Doubling the Excel artifact adds cost without demo value; the engine's rules are the same tested math, and a second workbook stays a candidate if the PA team wants it | Approved (revision 3) |
| 23 | 2026-08-06 | Entity tabs switch bundled fixtures and their IPS policy scope; an applied import overrides the active tab until reset or tab change | One-entity-per-file (V17) + tab switching gives real fund separation without a multi-entity contract | Approved (revision 3) |
| 24 | 2026-08-06 | Contract schema 1.1.0 carries quoted IPS policy structure (`policy_target` with explicit min/target/max/halfstep; `benchmark_definition` with numeric lag) — dataset-provided bands override the bundled pack | Policy travels with the data instead of living only in app code; 1.0.x files stay valid | Approved (revision 4) |
| 25 | 2026-08-06 | OPEB entity is workbook-derived like Pension (`--entity` parameterization of the builder/QA/exporter); the engine-only OPEB generator retired; both funds share one market-context series (dedicated market RNG) | One auditable path for both funds; two different "markets" on the two tabs would be wrong | Approved (revision 4) — supersedes #22 |
| 26 | 2026-08-06 | IA simplified per second external review (adopted ~90%): Pulse-first screen, 4+More navigation, evidence behind expanders, bullet-range allocation, root-cause-merged exceptions. Modifications: covered-basket return demoted not deleted; reconciliation one click away on Performance; hurdle kept with inline definition; licensing warning condensed but retained on Import | First screen was serving four purposes at once; preserve data, reveal detail on request | Approved (revision 5) |
| 27 | 2026-08-06 | Policy page removed from navigation; quoted IPS tables demoted to collapsed audit references on Methodology, with `#/policy` redirecting there | The page only restated the IPS — the PA team already owns that document, so it earned no navigation slot; the citations are kept (not deleted) because auditors and non-PA audiences still need to see where the Allocation bands come from | Approved (revision 5.5) |
| 30 | 2026-08-13 | Schema 1.2 provenance (`entered_by`/`reviewed_by`/`review_status`) with version-gated column sets and V19–V21; the audit log IS the file — no separate log store; public fixtures carry synthetic actor labels only | Team accountability without a backend: rows already carry who/when, extending the history-in-the-file architecture; real names never enter the public repo | Approved (revision 6) |
| 31 | 2026-08-13 | Exceptions carry tier (blocking/warning/informational) + days-open aging computed from in-file dates, sorted tier-then-age; allocation sleeves within 1.0 pp of a bound show an amber near-bound state (constant threshold until tolerance_definition lands) | Triage in under a minute needs severity and age at a glance; near-bound is an early warning distinct from a breach | Approved (revision 6) |
| 32 | 2026-08-13 | Reconciliation = recon_value source-side pairs + tolerance_definition records; V05 natural key includes source_name for recon rows only; variance always computed on screen; demo sides tie to the workbook's own EMVs with one deliberate break | Tolerance-as-data makes thresholds configurable without a settings store; importing a variance would let a file assert its own correctness | Approved (revision 7) |
| 33 | 2026-08-13 | ACFR board rebuilt on contract records in a single-entity tracker file (supersedes #15): one row per status change is the change log; artifacts are links+metadata only; viewer roles are soft and labeled demonstration-only; the Complete action emits a ready-to-append CSV row; enforceable tracker recommended for the internal M365 stack (hybrid) | A static site cannot enforce identity and must not pretend to; the file-is-the-record architecture gives the requested change tracking for free | Approved (revision 7) |
| 34 | 2026-08-13 | Private-markets records carry primitives only (commitment/called/distributed/NAV); unfunded, DPI and TVPI are computed in-app; lagged NAV always labeled; zero-called ratios render null | Imported ratios can silently disagree with their inputs — same derived-not-imported rule as over/under | Approved (revision 7) |
| 35 | 2026-08-13 | Architecture diagram recreated as a native inline SVG on Methodology (not the supplied PNG): raster embeds blur and the source diagram's tokens had drifted from the code (review_status "draft->pub", V01-V21); the SVG uses code-true tokens and LIVE/TARGET chips | The diagram is a governance artifact — it must match the implementation exactly, and the code wins on vocabulary | Approved (revision 7) |
| 36 | 2026-08-13 | ACFR page-level tie-out items nested under their section cards (RSI/SI map into Financial); crosswalk register removed, QA controls kept cross-section; architecture diagram removed from the public Methodology page (component deleted) | Items belong to sections — two flat registers made the reader do the joining; the diagram is a working/internal reference, not public content (owner decision) | Approved (revision 7.1) |
| 37 | 2026-08-13 | Adopted the owner's LACERA redesign: seven-view presentation layer quoting published FY2025 figures with per-panel citations; synthetic-workflow views retired from navigation while the contract engine, generators, and tests remain intact (ACFR still reads contract records) | The showcase product is now the published-figures story; the audited Excel→contract machinery stays the repo's core and can return to the surface when the team wants the live-workflow demo | Approved (revision 8 — owner-directed redesign) |
| 38 | 2026-08-13 | Mulish self-hosted via @fontsource instead of the design's Google Fonts CDN link | Keeps the zero-network-request engineering standard; identical rendering | Approved (revision 8) |
| 39 | 2026-08-13 | Import, Reconciliation, and Exceptions restored as a footer-linked "Team workflow demo" section on the LACERA design — synthetic contract data behind an explicit banner wall, main nav stays the seven design tabs | The presentation layer quotes published figures; the workflow section is the demonstrable pipeline proof the team-tool vision requires — mixing the two on one surface would blur the published/synthetic wall the governance model insists on | Approved (revision 8.1 — owner selected the three views) |
| 40 | 2026-08-13 | App split into Dashboard (6 presentation tabs, published figures) and Workstation (Data/Recon/Exceptions/ACFR, synthetic contract data) modes with a nav-bar switch; ACFR moved into the Workstation; the feed is demonstrated, not literal — stated on both sides | The workstation/dashboard split matches the team's operating model and the workflow diagram; a literal public feed would blur the published/synthetic wall, so it remains the internal version's story | Approved (revision 8.2 — owner-proposed structure) |
| 41 | 2026-08-13 | Trust controls are data-enforced gates, not copy: an explicit entity registry (`app/src/fixtures/entityRegistry.ts`) hard-blocks cross-entity staging and apply (E-ENTITY / E-UNREGISTERED / E-TRACKER; policy identity never inferred from name text); ACFR completion is eligibility-computed (tie-outs, artifacts, blockers, independent reviewer) with status-ahead-of-controls flagged; fund-vs-benchmark comparison fails closed on return_method/gross_net mismatch (blocking METHOD-* exceptions); blocking exceptions plus out-of-tolerance recons drive a demonstrated publication gate on workstation views | The audit showed one fund's file could stage into the other workspace and a section could complete with zero controls done; computing every gate from the data teaches the real control, while the static site never claims enforcement it cannot provide | Approved (revision 9) |
| 42 | 2026-08-13 | Honest labels and provenance: “Risk & Compliance” → “Policy Monitoring” and “Holdings & Managers” → “Holdings & Fees”; per-panel citations move to a typed source registry (`app/src/fixtures/sources.ts`) with real ACFR page anchors and deliberately no PAFR/IPS URLs; DateLine separates report/valuation/data-through/retrieved dates; TWR net-of-fees labeling with an explicit MWR non-comparability note; derived tables labeled “calculated from quoted figures” | The page monitors policy bands — no risk analytics exist; fee and top-10 math are derivations, not official ratios; fabricating document links would be worse than omitting them | Approved (revision 9) |
| 43 | 2026-08-13 | Verification and delivery hardening: dev-server fs.allow narrowed to the app + data/sample; the Pages workflow gates deploy on lint, format check, the vitest suite, and npm audit --omit=dev --audit-level=high; a Playwright smoke suite (desktop + 320/360/375) mechanically checks every route for page-level overflow, runs axe on the entry surfaces, and exercises the three demonstrated controls; jsdom component tests cover the E-ENTITY hook path and the disabled ACFR action | The workflow previously deployed whatever main held — a broken or vulnerable tree could ship; mobile overflow and control regressions are now mechanical failures instead of eyeball checks | Approved (revision 9) |
| 99 | 2026-09-06 | The published-figure literals (`app/src/fixtures/published.ts`) are covered by a consistency test (`published.test.ts`): statement identities, tile/flow/statement agreement, mix and IPS sums, fee arithmetic, and every derived sentence. A discrepancy the test finds is disclosed on the screen where the figure appears and logged as an open verification item — never corrected by inference (two found: the OPEB cumulative-NII FY2023 step of −$41.0M vs $248M of NII; the OPEB Real Assets ½-step sub-rows summing to 15.5% vs 16.5%) | Quoted numbers can only be checked against each other on a static site; a transcription slip that reaches a board deck is worse than a visible “verification open” note, and guessing the right number would break the never-assume rule | Approved (revision 10 audit) |
| 100 | 2026-09-06 | Source-registry ACFR links resolve through `acfrPage(printed)` (printed page + 2 = PDF index in the FY2025 file) and an `ACFR_RETURNS` record cites pp. 112–113; the Overview KPI tiles carry a `reported_public` badge and a source line; the assumed-rate sentence is computed against the decade-high assumed rate stored with each entity | All three ACFR links opened the wrong table, and a wrong link is worse than none; the headline tiles were the only Dashboard metrics without a visible classification or source, contrary to the every-metric-classified rule | Approved (revision 10 audit) |
| 101 | 2026-09-06 | Workstation integrity: a proxy's one-day return needs the observation immediately before it (`lastDailyReturn`; a missing prior close yields null, so read-through coverage falls instead of a multi-day move being booked as daily); contribution reconciliation is displayed on the Reconciliation view and a FAIL blocks the demonstrated publication gate. Shell: Workstation views are lazy-loaded behind a per-route error boundary, a print stylesheet is added, and axe runs on all ten routes in the smoke suite | The reconciliation rule is the project's signature control and was computed but invisible; the gap splice mislabelled a multi-day move as daily; a throwing view blanked the whole shell; the axe gate covered two routes | Approved (revision 10 audit) |
| 102 | 2026-09-07 | Open items O1/O2 closed against the source documents: the OPEB cumulative-NII FY2021/FY2022 values are corrected from the PAFR p. 7 bar positions (a verified transcription error, not an inference); the OPEB IPS Table 1 ½-step sub-rows (15.5 vs 16.5) are a discrepancy in the source itself and stay as printed with an on-screen note. The PAFR and both IPS documents now carry verified links in the source registry (`IPS_OPEB_T1` added so OPEB views cite the OPEB document) | A figure is corrected only when the source itself shows the right value — here by label coordinates against the chart axis; a source's own arithmetic gap is disclosed, never repaired. Decision 42 withheld PAFR/IPS links because they were unverified; they are now verified against downloaded copies, so omitting them would be the weaker citation | Approved (revision 10.1) |
| 103 | 2026-09-07 | The CIO Monthly deck and the dashboard share one fixture (`app/src/fixtures/cioMonthly.data.ts`, lifted verbatim from the deck by evaluating its own data block, not by retyping). The deck stays a self-contained file at /deck/ because people open it directly, so its data block is generated from the fixture (`npm run sync:deck`) between marker comments, and a unit test fails whenever the block on disk differs from the fixture. The deck is not rewritten in React | Two hand-maintained copies of the same figures had already diverged in format and would diverge in value; generating the block keeps the single-file deck (offline, USB, print, presenter view) while making the fixture the only place numbers are edited | Approved (revision 11 — user chose integration over replacement) |
| 104 | 2026-09-07 | The monthly report is a separate vintage: a “CIO Monthly” dashboard tab with its own masthead date, notice bar, date line and an explicit note; no panel mixes CIO Monthly figures (market value at May 31, 2026; monthly periods) with the FY2025 PAFR/ACFR figures (fiduciary net position at June 30, 2025; fiscal-year horizons). The deck's gap attribution keeps its `proxy_estimate` badge and visible residual on the tab | The financial-integrity rules forbid silently combining reporting vintages, and a proxy must never read as an attribution result | Approved (revision 11) |
| 105 | 2026-09-07 | CIO Monthly figures are extracted from the public PDFs by `tools/extract_cio_report.py` (pages found by content, cells read by word coordinates against header anchors) and a report is accepted only when it passes the identities it prints; a failing or image-only report is rejected with the reason and left out of the series, never patched. A report-side unitemized cash line (≤0.3% of the fund, matching a gap in the printed weights) is kept as an explicit "Not itemized" line | Text-order reads swapped the OPEB cumulative series once already; a coordinate read with printed identities is the only way to add sixteen months without a transcription risk per month, and an excluded month is more honest than a guessed one | Approved (revision 12) |
| 106 | 2026-09-07 | The CIO Monthly tab keeps every extracted report as its own vintage selected in the URL; the masthead, notice bar and band follow the selection; changes are computed only against the immediately prior report and labelled calculated; citations are built per vintage. The deck always shows the latest report and its prose is generated from a `VINTAGE` record (no month or report date is hardcoded) with the executive sentences computed from the data | A monthly product must answer "what changed since last month" without ever mixing months in one figure; hand-written findings that held for one report would silently become wrong the next month | Approved (revision 12) |
| 107 | 2026-09-07 | Interactivity is added only where it moves a reader from a headline to its evidence or between reports: tile → panel jumps, a Returns/Excess view, a selectable attribution period, panel → deck-slide links, and definition tooltips on classification badges. Animation, count-ups and decorative charts are not added to the dashboard | The audience is an investment office reading for decisions; the deck already demonstrates motion, and the dashboard's value is trust and drill-down, not spectacle | Approved (revision 12.1) |
| 108 | 2026-09-07 | The CIO Monthly composites table shows the IPS range and distance to the nearer bound against the month-end weight. This is a comparison of the policy in force with an actual as printed — labelled calculated, cited to the IPS, and stated not to be a compliance judgement — not a mixing of reporting vintages | The IPS is a governing document rather than a dated data vintage; the monthly report itself declines to judge ranges, so the tab makes the basis and the limits of the comparison explicit instead of hiding a useful check | Approved (revision 12.1) |
| 109 | 2026-09-07 | The CIO Monthly layer gets a contract path (schema 1.4 `cio_monthly`, rule V24) so the internal version can feed the tab — and, there, the deck — from a workstation export instead of a PDF; the same identities the extractor enforces on a report are enforced on the rows, and the public sample is generated from the latest extracted vintage and round-trip tested so the two paths cannot disagree | The hosting plan's "always-on" story needs a data path, not a PDF path; enforcing the report's arithmetic at import means a bad export is rejected at the door rather than rendered | Approved (revision 12.2) |
| 110 | 2026-09-07 | An imported feed is shown as its own "Workstation dataset" vintage with the publication gate and its rows' classification on screen, never merged into the public series; on the public site the deck keeps the latest public report | A workstation feed is operational data under review, not a published report; the reader must always see which it is | Approved (revision 12.2) |
| 111 | 2026-09-07 | Visual regression runs from a separate Playwright config with baselines under `outputs/visual-snapshots/` (ignored) and is invoked on demand; the default smoke suite and the Pages workflow never depend on screenshot baselines | Baselines are platform-specific and change with every legitimate monthly update; a mechanical pixel check is valuable when an analyst runs it after `cio:diff` and reviews the diff, and harmful as a CI gate that fails on fonts and rendering drift | Approved (revision 12.3) |
| 112 | 2026-09-07 | The Overview opens with the latest monthly report as a separate strip above a labelled fiscal-year divider: its own report date, its own citation, and an explicit sentence that a monthly market value is not the fiscal-year fiduciary net position. The masthead keeps the fiscal-year as-of on that tab | The freshest decision-relevant figures were behind the seventh tab while the front door showed figures a year old; two vintages may share a page when each block carries its own date and the difference is stated, which is the same rule the CIO tab already follows | Approved (revision 13) |
| 113 | 2026-09-07 | The two-minute read and the change list are COMPUTED from the report's figures (`lib/cioNarrative.ts`, unit-tested), never written per month, and every sentence links to the panel that evidences it; the attribution sentence inherits the proxy-estimate classification | A narrative written for one month silently becomes false the next; a computed one cannot outlive its data, and a reader who cannot reach the evidence has been told rather than shown | Approved (revision 13) |
| 114 | 2026-09-07 | View state that changes what a panel shows — fund, selected report, Returns/Excess, attribution period, trend view, compare — lives in the URL, and `EntityProvider` moved inside the router to make the fund URL-backed | A pasted link is how this audience shares a finding; state that lives only in component memory sends the recipient to a different screen than the sender saw | Approved (revision 13) |
| 115 | 2026-09-07 | Every panel that holds a table offers "copy table as CSV" (values exactly as displayed, leading =/+/−/@ escaped against formula injection); the action is hidden by `.panel:not(:has(table))` rather than by a render-time DOM probe | Analysts retype figures into Excel or reopen the PDF; copying what is on screen keeps the displayed value and its rounding intact. Presence is a styling question, and a CSS rule avoids a state-setting effect on every panel | Approved (revision 13) |
| 116 | 2026-09-16 | The web dashboard and the deck take the CIO Monthly Report's visual identity — title-bar navy, section bands, cyan fund against dark-blue benchmark, the allocation pie's category colours, Arial, charcoal footer — with every value extracted from the report's own drawing fills. The Power BI pilot keeps the ACFR-derived theme of decision 74 | The monthly report is the document the Board already reads, so the dashboard should look like its companion; extraction rather than eyeballing keeps the match exact and reproducible. The Power BI pilot is an ACFR tool and was deliberately themed to the ACFR | Approved (user request) |
| 117 | 2026-09-16 | Economic data enter the public site as a dated FRED snapshot generated by `tools/fetch_macro_snapshot.py`, never as a live call; derived figures are computed in tested TypeScript from raw observations | A static Pages site cannot hold an API key, and a key in the bundle is a published key. A dated snapshot is auditable and reproducible, and keeping raw observations (not pre-computed readings) lets the tests check the arithmetic | Approved (user request) |
| 118 | 2026-09-16 | Only series whose FRED notes carry no redistribution restriction are published; ICE BofA spreads, Moody's Baa, IMF copper and University of Michigan sentiment are excluded, listed on the page with the reason, and re-checked on every refresh. Credit conditions use the Chicago Fed credit and risk subindexes and the St. Louis Fed stress index instead of corporate spreads | Both source dashboards used series whose owners prohibit reproduction; CLAUDE.md forbids assuming a public source may be republished. A disclosed substitution is honest; a silent one is not | Approved |
| 119 | 2026-09-16 | The regime read is two separate reads: level (growth and inflation z-scores, naming a quadrant only outside ±0.5 σ) and direction (the ChatGPT dashboard's three-month rules on core PCE, unemployment and payrolls, suppressed when an input is stale) | The two dashboards answered different questions and could disagree; labelling a −0.05 σ growth reading "Stagflation" overstates what the data say | Approved |
| 120 | 2026-09-16 | The portfolio lens applies stated sensitivities to the selected fund's IPS Table 1 long-term targets and is classified proxy_estimate, with the full grid, the factor arithmetic for any row and the limitations on the page. Two rows (Non-Core Real Estate, Diversified Hedge Funds) were added to cover every IPS sleeve and are marked | Sensitivities are assumptions, not estimates; showing them lets a reader challenge them. Policy targets are public and fund-specific, whereas the source dashboard's scaled sub-weights were unverified and omitted hedge funds | Approved |
| 121 | 2026-09-16 | The tab's nav label is "Economy" (band title "Economic context"), and desktop nav gaps are 20 px | Eight views plus the mode switch otherwise wrap at conference-room widths; the shorter label keeps one row with 18 px to spare at 1440 px | Approved |
| 122 | 2026-09-17 | A page states its default classification and page-wide sources once (`PageMeta`, `AboutFigures`, `PageSources`); badges mark only figures that differ, citations are chips that open the full reference, and method text sits behind "How this is calculated" | Small print outweighed readable text three to five times on every tab. CLAUDE.md requires every figure to be classified and cited, not that the label repeat beside it; a page statement plus exceptions keeps the rule and one click keeps the audit trail | Approved (user request) |
| 123 | 2026-09-17 | CIO Monthly and Economy are split into URL-backed sub-tabs, each opening on a one-screen summary; jumps and panel links open the right tab | Seven-plus screens and 13–16 panels on one page defeated the two-minute read; tabs keep the drill-down one click away and shareable | Approved (user request) |
| 124 | 2026-09-17 | Motion is used only to show a change (draw-in on first view, geometry moving to new values, a one-time flash on a changed figure), never to decorate; nothing counts up; all of it is disabled under `prefers-reduced-motion`, and the test and baseline configs run with reduced motion | A count-up displays numbers no report printed; motion that tracks a change helps the eye compare; tests must read the settled page | Approved (user request) |
| 125 | 2026-09-17 | Interactive controls (chart tooltips, legend toggles, linked highlights, report slider, zoom) duplicate information already on the page as text or tables; each chart is one tab stop read with the arrow keys | A tooltip must never be the only way to read a value, and a chart with a focusable point per value would flood the tab order | Approved (user request) |
| 126 | 2026-09-17 | The Economy what-if control changes only the latest factor readings, keeps them in the URL, and marks the lens "Scenario — not observed" with a reset; results stay proxy_estimate | A scenario is an assumption set by the reader; it must never be mistaken for observed data or a forecast | Approved (user request) |
| 127 | 2026-09-17 | The dashboard feeds the CIO slides: the Present sub-tab loads the unchanged deck page in a same-origin frame and hands it the data for the report on screen (`deckDataFor`, read once from the parent at start); fund and slide sync by origin-checked messages; imported text is stripped of < > " on both sides; editorial slides say when their pages exist only for the latest report. The standalone `/deck/` keeps its generated latest-report block | Presenting the deck as it already works avoids a second slide implementation drifting from the first, and makes any report or an imported feed presentable without regenerating files; the frame keeps the standalone file's offline guarantee. Imported files are untrusted input to HTML-built slides | Approved (user request) |
| 128 | 2026-09-17 | The CIO Monthly tab opens on the slides (default sub-tab "Slides"), the page's arrow keys step them when no control has the keyboard, and the band offers "Present full screen"; the one-screen Summary is the second sub-tab | The monthly report is presented as slides, and the reader asked to go through them without leaving the dashboard; opening the tab on the deck removes the click-out, while the figures behind each slide stay one tab away | Approved (user request) |
| 129 | 2026-09-18 | The CIO macro strip's PCE inflation, federal funds target range and unemployment and participation rates come from FRED for every report, read from FRED's real-time archive (ALFRED) as of the month end before the report's month; the dollar index, themes and the report's commentary stay typed for the latest report, and the printed values are kept to cross-check FRED in a unit test | The report's macro page cites the St. Louis Fed, and those figures are public FRED series; reading them as known on the as-of date (not today's revised values) reproduces the August 2026 report's printed figures exactly, where the meeting date would not (it picks up July's labor data), and gives the strip for all 16 reports instead of the latest only. The report's U.S. Dollar Index is not a FRED series, so no substitute index is shown under its name | Approved (user request) |
| 130 | 2026-09-18 | On slide 7 the index chart tightens its rows when the macro strip is shown (and for print), so the strip clears the source line | The strip overlapped the source note even before this change; shrinking the chart only while the strip is shown keeps the full-height chart for the first build |  |
| 131 | 2026-09-18 | The "How this report works" guide is a static page on the dashboard site (`/how-it-works/`), linked from the CIO Monthly tab, rather than a link to a claude.ai artifact | The team needs a link they can open without a claude.ai account, and the guide should live and change with the code it describes; a public page in the repository satisfies both, and it states it is not an official LACERA publication | Approved (user request) |
| 132 | 2026-09-18 | Teammates add a CIO report through a GitHub Actions workflow started with the report's lacera.gov link, which proposes the update as a pull request; the site itself takes no uploads | The site is static, so it cannot store an upload for everyone; GitHub is the only shared place that can run the extractor. Taking the lacera.gov link instead of an uploaded file avoids republishing LACERA's PDFs in the repository, the link check limits downloads to lacera.gov, a pull request keeps a person between extraction and publication, and the typed parts stay a human step that the tests enforce. The FRED key lives only as a repository secret | Approved (user request) |
| 133 | 2026-09-18 | The team can build the CIO slides from an Excel template's CSV export, read in the browser only: held in memory, never stored or sent, cleared on reload; the whole file is checked and a file with any problem shows nothing | Before publication the figures are confidential and the site is public and static: the file can only be read locally, and keeping it out of browser storage and the address means nothing lingers on a shared machine. Checking the whole file first follows the rule that malformed input fails clearly without partial results |  Approved (user request) |
| 134 | 2026-09-18 | Template figures are shown as `calculated`, every source line names the template file and says "not published", and the macro strip is the file's own (FRED figures are for published reports) | They are entered by the team, not quoted from a public source for the stated period, so `reported_public` would overstate them; saying where they came from on every slide keeps a rehearsal deck from being mistaken for the published one |  |
| 135 | 2026-09-18 | A figure the input did not supply is `null` and shown as "not supplied"; nets and statistics are computed only from complete inputs; a template gives every flow or none | A missing observation does not establish that nothing happened; defaulting to zero turned absence into a reported $0M and a zero-month tail (audit finding 1) |  |
| 136 | 2026-09-18 | An imported feed is labelled with its rows' own classification — the most cautious one when mixed, and an unknown class as synthetic — and named by its dataset ID on page and slides | The page's default was reported_public, which promoted synthetic imports to public status; the rows' stated source can be the public report they only re-express (audit finding 2) |  |
| 137 | 2026-09-18 | Report-over-report comparisons require both data-through dates; across a fiscal-year turn FYTD is a reset, not a change, and across January so is YTD | FYTD covers different periods on either side of July 1; ranking the difference as a change showed a −8.7 pp "deterioration" that was only a new year (audit finding 4) |  |
| 138 | 2026-09-18 | The market table is named with its own as-of dates, and the histogram gives a bracket (months lower, months in the same range) instead of a percentile | The market table is a month later than the fund figures, and grouped bins cannot rank a month within its bin (audit findings 5 and 6) |  |
| 139 | 2026-09-18 | Dependency advisories are cleared with the smallest upgrades that fix them (Vite 6.4.3, Vitest 4.1.11, React Router 7.18.4), not the newest majors; the browser suite gates deploy and the full audit is reported, not blocking | The advisories were in development servers and client-side routing, not the static output, so the goal was removing them at the least risk; a new development-tool advisory should be seen on every run without stopping a static-site release, while the production audit keeps blocking |  |
| 140 | 2026-09-18 | A workbook is opened by turning one sheet into CSV text and reading that with the existing CSV readers. Values are taken as stored. Uncalculated formulas and Excel error cells are refused | A second reader for workbooks would be a second set of rules to keep in step. Converting to the CSV Excel would write keeps one validator. Reading displayed text would carry the display format into the data (1.23% instead of 0.0123), and reading a formula with no saved result as blank would pass off missing figures as empty ones |  |
| 141 | 2026-09-18 | SheetJS CE 0.20.3 is installed from SheetJS's own tarball with an integrity hash and bundled as a lazily loaded chunk, not loaded from a CDN | The registry package is stale and has advisories. A CDN script would add a third-party origin to a page that reads confidential files. A same-origin chunk is covered by the site's own build, audit and integrity pinning, and only costs download time when someone opens a workbook |  |
